[workspace]
members = ["geoip-compat", "glibc-compat"]
resolver = "3"

[workspace.package]
version = "0.1.0"
edition = "2024"
authors = ["llnut"]

[profile.release]
opt-level = 3
lto = true
strip = true
panic = "abort"
